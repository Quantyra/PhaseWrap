# Q-RoPE Epic Portfolio v5

## BLUF
- The Q-RoPE research program remains one program with multiple stretch-effort epics.
- `E001` remains the preserved foundation epic.
- `E002`, `E003`, and `E004` are preserved bounded stretch-effort results.
- `E005` is now an archived negative boundary epic.
- `E006` is now opened as the next memo-only stretch effort: bounded multi-hop positional reference resolution.

## Program Structure
- program:
  - Q-RoPE research program
- epics:
  - `E001` foundation and internal evidence package
  - `E002` bounded positional selection robustness
  - `E003` bounded position-content compositionality
  - `E004` bounded content-alias disambiguation
  - `E005` bounded shared-memory multi-query selection
  - `E006` bounded multi-hop positional reference resolution

## Epic Roles
### E001
- preserve benchmark, transfer, bridge, realism-bridge, and initial successor-class evidence
- maintain the standing internal package

### E002
- test whether successor-class signal survives bounded candidate-set variability
- preserve or stop based on bounded robustness evidence

### E003
- test whether the signal survives bounded composition of positional structure with content-key relevance
- preserve or stop based on bounded position-content evidence

### E004
- test whether the signal survives bounded content alias pressure where multiple candidates share the same content class
- preserve or stop based on bounded alias-disambiguation evidence

### E005
- tested whether the signal survives multiple distinct query-conditioned retrieval decisions over one shared bounded candidate memory
- archived as a negative boundary after failure at retained nuisance hardening `token_permutation=cdab`

### E006
- test whether the signal survives bounded two-stage positional reference resolution through an intermediate candidate
- stop immediately if the task collapses into direct lookup, unbounded latent pointer tables, or fairness blow-up

## Why This Structure
- The repo now preserves bounded evidence through content-alias disambiguation and preserves an archived boundary on shared-memory multi-query reuse.
- The next materially different gap is bounded multi-hop reference resolution, not another one-shot or repeated-query reuse variant.
- Opening `E006` keeps the next major compositional realism-adjacent question visible at the epic level while leaving execution closed until gates are passed.
