# Transition Orbit Approval-Candidate v1

## Status
- `approval-candidate`
- still memo-only
- not implementation-approved

## Why this angle is preserved
- The token-invariant line failed because invariance diagnostics passed but the witness still lost to bounded controls on the primary metric.
- The remaining weakness is that token views were still exposed through duplicated rendered examples.
- The orbit task removes that degree of freedom at task definition time.

## Remaining blocker
- future orbit-additive symbolic baseline must be specified before any implementation approval gate is legitimate.
